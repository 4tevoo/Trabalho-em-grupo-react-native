export interface StatItem {
  value: string;
  label: string;
}

export interface SectionItem {
  icon: string;
  heading: string;
  body: string;
}

export interface AudienceItem {
  id: string;
  icon: string;
  label: string;
  description: string;
}